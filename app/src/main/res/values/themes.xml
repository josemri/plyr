<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Plyr" parent="android:Theme.Material.Light.NoActionBar" />
    
    <!-- Splash Screen Theme - Negro al arrancar, sin dependencia core-splashscreen -->
	<style name="Theme.Plyr.SplashScreen" parent="android:Theme.Material.Light.NoActionBar">
		<item name="android:windowBackground">@android:color/black</item>
    </style>
    
    <!-- Fallback splash theme for older Android versions -->
    <style name="Theme.Plyr.SplashScreen.Fallback" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>

